Required libraries:

pip install scrapy

Program can be executed in terminal with the following command:
scrapy runspider galador_spider_scrapy.py

After running the program, graphics_cards_scrapy.json file is created to project folder.